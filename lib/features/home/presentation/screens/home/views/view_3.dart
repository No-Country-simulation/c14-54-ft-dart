import 'package:flutter/material.dart';

class View3 extends StatelessWidget {
  const View3({super.key});
  static const route = 'view_3';

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('View3'),
      ),
    );
  }
}
